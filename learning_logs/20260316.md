# 입실 체크

# 팀 프로젝트 전 일정 사항 (변동 가능성 있음)
1. 3월 27일 - 3개 과목 시험 및 팀 공지 / 회식
2. Back - Front 연결 이후 수업 사항
  1. OAuth2 (Google 로그인 기준) 구현
  2. AWS (Amazon Web Service) 기준 배포 (SpringBoot)

# 팀 프로젝트 이후 사항
1. 4월 첫째 주 경 기획 발표 (주제 발표 및 주제 선정 배경 / 구현 기능 등)
2. 강사 피드백 후에 같은 PT (혹은 추가자료)로 재발표 (서로 다른 발표자가 발표할 것)
3. 팀 프로젝트 시작

# PUT 요청 (Modal에 기존 정보가 그대로 있을 것이기에)
1. 각 테이블의 row에 현재 delete 버튼만 존재하는데, Edit 버튼을 추가하여 수정 기능 구현
   -> 그렇다면 어느 컴포넌트에서 Edit 버튼이 필요한가, 어떻게 설정할 수 있는가를 고민. 

2. 해당 버튼을 눌렀을 떄 사용자가 기존 자동차를 편집하고 변경사항을 저장할 수 있도록 모달이 열릴 것.
   그리고 그 모달창은 POST 요청과 동일한 템플릿이 될건데,
   차이점이 있다면 특정 row의 Edit 버튼을 눌렀을 때 활성화되는 Dialog 컴포넌트에는
   수정 대상 차량의 정보가 미리 입력되도록 하겠음
   (필요한 부분만 수정할 수 있게끔.)

3. components 폴더에 EditCar.tsx 컴포넌트를 생성하고 초기화
```tsx
import { useState } from "react";
import { CarResponse, Car } from "../types";

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({ cardata } : CarResponse) {
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  })

  return(
    <>

    </>
  );
}
```
- 이상의 코드에서 다양한 부분을 설명
  AddCar.tsx의 경우 href(고유값)이 아직 생성되지 않았기에 props drilling이 요구되지 않지만
  EditCar.tsx에는 특정 자동차 정보가 있고 그 자동차 정보를 수정하는 것이기에 
  객체 구조분해를 활용해야하고, 그 자료형이 CarReponse이기에 `FormProps`라는 타입을 별개로 정의

- 허나, FormProps의 경우 EditCar 컴포넌트 내부에서만 사용될 것이기에 굳이
  types.ts로 분리해주지 않음. 즉, 재사용을 하게 될 자료형이라면 별개의 파일에서 export 하겠지만
  단일한 곳에서만 사용된다면 굳이 외부 파일에서 import 해올 필요가 없다는 의미

- 그리고 상태를 선언해서 `<Car>`로 작성했기에 AddCar 컴포넌트의 상태를 그대로 복사해옴.
  차 한대 추가 / 차 한대 수정에서 '차 한대'라는 공통점을 가지고 있고,
  state의 자료형이 `<Car>`로 동일하기에 복사해와도 문제가 없을 것 같음.

```tsx
import { useState } from "react";
import { CarResponse, Car } from "../types";
import { Dialog, DialogActions, DialogTitle } from "@mui/material";

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({ cardata } : CarResponse) {
  const [ open, setOpen ] = useState(false);
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  });

  const handleClickOpen = () => {
    setOpen(true);
  }

  const handleClickClose = () => {
    setOpen(false);
  }

  const handleSave = () => {
    setOpen(false);
  }

  return(
    <>
      <button onClick={handleClickOpen}>
        Edit
      </button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>Edit Car</DialogTitle>
        
        <DialogActions>
          <button onClick={handleClickClose}>Cancle</button>
          <button onClick={handleSave}>SAVE</button>
        </DialogActions>
      </Dialog>
    </>
  );
}
```
- 현재까지의 작성 영역을 봤을 때, DialogTitle과 DialogActions 사이에 수정할 내용을
  입력 받을 input 창들이 필요하다는 것을 알 수 있음.
  그리고 저희는 AddCar에서 그걸 가지고 오기로 했음.

- 현재 타이밍에서 input 창들을 모듈화해야하겠다는 생각이 들면 좋음.
- 그러면 EditCar 부분을 잠깐 멈추고, 모듈화를 한 다음, 완성한 input 창들 (즉 form)을
  export 시켜서 EditCar에서 그냥 그대로 가져와 재사용하겠음.

- New Car 폼의 텍스트 필드를 렌더링하는 새로운 컴포넌트를 생성할 것,
  그리고, New Car와 Edit 폼 모두에서 이용할 수 있도록 하겠음.
  components 폴더 내에 CarDialogContent.tsx를 생성하시오.

```tsx
import { Car } from "../types";
import { DialogContent } from "@mui/material";

type DialogFormProps = {
  car : Car;
  handleChange : (e : React.ChangeEvent<HTMLInputElement>) => void;
}

export default function CarDialogContent({ car, handleChange }: DialogFormProps) {
  return(
    <>
      <DialogContent>
          <input type="text" placeholder="Brand" name="brand" value={car.brand} onChange={handleChange} />
          <br />
          <input type="text" placeholder="Model" name="model" value={car.model} onChange={handleChange}/>
          <br />
          <input type="text" placeholder="Color" name="color" value={car.color} onChange={handleChange}/>
          <br />
          <input type="text" placeholder="RegistrationNumber" name="registrationNumber" value={car.registrationNumber} onChange={handleChange}/>
          <br />
          <input type="number" placeholder="ModelYear" name="modelYear" value={car.modelYear} onChange={handleChange}/>
          <br />
          <input type="number" placeholder="Price" name="price" value={car.price} onChange={handleChange}/>
          <br />
        </DialogContent>
    </>
  );
}
```

```tsx
import { Dialog, DialogActions, DialogContent, DialogTitle } from "@mui/material";
import { Car } from "../types";
import { useState } from "react";
import { addCar } from "../api/carapi";
import { useMutation ,useQueryClient } from "@tanstack/react-query";

import CarDialogContent from "./CarDialogContent";

export default function AddCar() {
  const queryClient = useQueryClient();

  const { mutate } = useMutation(addCar, {
    onSuccess: () => {
      queryClient.invalidateQueries(['cars']);
    },
    onError: err => console.log(err),
  });
  const [ open, setOpen ] = useState(false);
  const [ car, setCar ] = useState<Car>(
    {
      brand: '',
      model: '',
      color: '',
      registrationNumber: '',
      modelYear: 0,
      price: 0
    }
  );

  // 한 줄 짜리라서 필요없을 것 같지만
  const handleClickOpen = () => setOpen(true);
  const handleClickClose = () => setOpen(false);
  
  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) =>
  {
    setCar({...car, [e.target.name]: e.target.value});
  }

  const handleSave = () => {
    mutate(car);    // 얘가 carapi.ts에 있는 addCar() 함수에 해당
    setCar({
      brand: '',
      model: '',
      color: '',
      registrationNumber: '',
      modelYear: 0,
      price: 0
    });
    handleClickClose();
  }

  return (
    <>
      <button onClick={handleClickOpen}>New Car</button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>
          New Car
        </DialogTitle>
        <CarDialogContent car={car} handleChange={handleChange} />
        <DialogActions>
          <button onClick={handleClickClose}>Cancle</button>
          <button onClick={handleSave}>Save</button>
        </DialogActions>
      </Dialog>
    </>
  );
}
```

- 이상의 두 개의 파일에서 수정함. AddCar에서 `<DialogContent>` 컴포넌트 부분을 다 잘라서
  CarDialogContent 컴포넌트의 return 부분에 집어넣음.
  그러면서 `{car, handleChange}`를 집어넣음.
  그래서 AddCar에서도 props drilling을 해줘야하기에 `<CarDialogContent car = {car} handleChange = {handleChange} />`와
  같은 return문 부분이 추가됨.
  `car = ` 부분은 props drilling, `{car}` 부분은 상태 및 함수에 해당한다는 것을 다시 한 번 확인바람.

```tsx
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { getCars, deleteCar } from "../api/carapi";
import { DataGrid, GridCellParams, GridColDef } from "@mui/x-data-grid";
import { Snackbar } from "@mui/material";
import { useState } from "react";
import AddCar from "./AddCar";
import EditCar from "./EditCar";

export default function Carlist() {
  const [ open, setOpen ] = useState(false);
  const queryClient = useQueryClient();

  const columns: GridColDef[] = [
    {
      field: 'brand',
      headerName: '브랜드',
      width: 200,
    },
    {
      field: 'model',
      headerName: '모델',
      width: 200,
    }, 
    {
      field: 'color',
      headerName: '색상',
      width: 200,
    }, 
    {
      field: 'registrationNumber',
      headerName: '차량번호',
      width: 200,
    }, 
    {
      field: 'modelYear',
      headerName: '모델연도',
      width: 200,
    },
    {
      field: 'price',
      headerName: '가격',
      width: 200,
    },
    {
      field: 'edit',
      headerName: '',
      sortable: false,
      filterable: false,
      disableColumnMenu: true,
      renderCell: (params: GridCellParams) => <EditCar cardata={params.row} />
    },
    {
      field: 'delete',
      headerName: '',
      sortable: false,
      filterable: false,
      disableColumnMenu: true,
      renderCell: (params: GridCellParams) => (
        <button 
          onClick={() => {
            if(confirm(`${params.row.brand}의 ${params.row.color} ${params.row.model}를 삭제하시겠습니까?`))
            mutate(params.row._links.self.href)}}>
          Delete
        </button>
    )
    }
  ];

  const { data, error, isSuccess } = useQuery({
    queryKey: ['cars'], 
    queryFn: getCars
  });

  const { mutate } = useMutation(deleteCar, {
    onSuccess: () => {
      setOpen(true);
      // 자동차 삭제 후 실행되는 로직
      queryClient.invalidateQueries({ queryKey: ['cars'] })
    },
    onError: err => {
      console.log(err);
    },
  })

  if (!isSuccess) {
    return <span>Loading....⏱️</span>
  }
  else if (error) {
    return <span>🚫자동차 데이터를 가져오던 중 오류발생🚫</span>
  } else {
    return(
      <>
        <AddCar/>
        <DataGrid
          rows={data}
          columns={columns}
          disableRowSelectionOnClick
          getRowId={row => row._links.self.href}
        />
        <Snackbar 
          open={open}
          autoHideDuration={2000}
          onClose={() => setOpen(false)}
          message = '해당 차량 정보가 삭제🚓'
        />
      </>
    );
  }
}
```

```tsx
import { useState } from "react";
import { CarResponse, Car } from "../types";
import { Dialog, DialogActions, DialogTitle } from "@mui/material";
import CarDialogContent from "./CarDialogContent";

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({ cardata } : FormProps) {
  const [ open, setOpen ] = useState(false);
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  });

  const handleClickOpen = () => {
    setCar({
      brand: cardata.brand,
      model: cardata.model,
      color: cardata.color,
      registrationNumber: cardata.registrationNumber,
      modelYear: cardata.modelYear,
      price: cardata.price
    });
    setOpen(true);
  }

  const handleClickClose = () => {
    setOpen(false);
  }

  const handleSave = () => {
    setOpen(false);
  }

  const handleChange = (e : React.ChangeEvent<HTMLInputElement>) => {
    setCar({...car, [e.target.name]: e.target.value});
  }

  return(
    <>
      <button onClick={handleClickOpen}>
        Edit
      </button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>Edit Car</DialogTitle>
        <CarDialogContent car = {car} handleChange={handleChange} />
        <DialogActions>
          <button onClick={handleClickClose}>Cancle</button>
          <button onClick={handleSave}>SAVE</button>
        </DialogActions>
      </Dialog>
    </>
  );
}
```

- 이상에서 중요한 부분은 handleClickOpen 함수 내에 setCar()를 통해 특정 row의 
  차량 정보를 car 상태에 저장한 부분임. 이상의 함수를 통해 모달이 열리기 전에 특정 차량 상태가 저장되고,
  textfield 에 표시되게 됨.

- 그리고 수정창이 열리는 것까지는 구현했는데 handleSvae 부분에 차량 정보를
  업데이트하고 DB에 저장하는 부분이 구현 안됨.

- axios 활용하고 put 요청을 통해서 http://localhost:8080/api/vehicles/[특정한자동차id] 에 보내야겠음.
  즉, deleteCar와 동일한 url이 될 것.
  허나, deleteCar의 경우에는 URL만 argument로 잡으면 되는 반면, 수정 내용도
  전달되어야 하기에 addCar() 함수와 유사한 점도 있음.

- URL도 보내야하고, 변경 사항도 보내야 하기에 type을 설정하겠음.
  types.ts에 CarEntry 자료형을 정의하겠음.
    - 그 이유가 있는데... react query에서 커스텀 함수의 경우 매개변수를 하나 밖에 못받아서 그렇슴...
    `(매개변수1: 자료형1, 매개변수2: 자료형2) => {구현부}` 이렇게가 불가능하다는 의미.

```tsx
import { useState } from "react";
import { CarResponse, Car, CarEntry } from "../types";
import { Dialog, DialogActions, DialogTitle } from "@mui/material";
import CarDialogContent from "./CarDialogContent";
import { updateCar } from "../api/carapi";
import { useMutation, useQueryClient } from "@tanstack/react-query";

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({ cardata } : FormProps) {
  const [ open, setOpen ] = useState(false);
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  });
  
  const queryClient = useQueryClient();
  const { mutate } = useMutation(updateCar, {
    onSuccess: () => {
      queryClient.invalidateQueries(['cars']);
    },
    onError: (err) => {
      console.log(err);
    }
  });

  const handleClickOpen = () => {
    setCar({
      brand: cardata.brand,
      model: cardata.model,
      color: cardata.color,
      registrationNumber: cardata.registrationNumber,
      modelYear: cardata.modelYear,
      price: cardata.price
    });
    setOpen(true);
  }

  const handleClickClose = () => {
    setOpen(false);
  }

  const handleSave = () => {
    const url = cardata._links.self.href;
    const carEntry : CarEntry = {car, url};
    mutate(carEntry);
    setCar({
      brand: '',
      model: '',
      color: '',
      registrationNumber: '',
      modelYear: 0,
      price: 0
    })
    setOpen(false);
  }

  const handleChange = (e : React.ChangeEvent<HTMLInputElement>) => {
    setCar({...car, [e.target.name]: e.target.value});
  }

  return(
    <>
      <button onClick={handleClickOpen}>
        Edit
      </button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>Edit Car</DialogTitle>
        <CarDialogContent car = {car} handleChange={handleChange} />
        <DialogActions>
          <button onClick={handleClickClose}>Cancle</button>
          <button onClick={handleSave}>SAVE</button>
        </DialogActions>
      </Dialog>
    </>
  );
}
```
- useMutation() / useQueryClient의 사용과 관련된 부분을 복습바람.
  useQuery()와 useMutation()의 차이가 됨.

# CSV로 데이터 내보내기
- AG-grid가 아니라 x-data-grid를 도입한 이유는 
  얘가 기본적으로 csv로 보내기 기능을 지원하기 때문임.

## MUI 아이콘 및 IconButton 컴포넌트 활용
npm install @mui/icons-material@5.17.1

https://mui.com/material-ui/material-icons/

- 이상까지 했을 때 MUI를 기반으로 한 컴포넌트 선택 방식과,
  mui-icons / lucide-react를 통한 아이콘 도입을 시도했음.

- AI 사용 시 tailwind를 쓰게 될 확률이 높은데, 도입 방식 및 원리에 대해서는 팀 프로젝트 시 학습 바람

- 클래스에 이름 갖다 붙이면 tailwind가 알아서 꾸며주는 형태가 기본 원리인데 내부의 설정값이 엄청 많기에 본시 수업에서 사용 안함.

# Frontend에서의 Application Protection